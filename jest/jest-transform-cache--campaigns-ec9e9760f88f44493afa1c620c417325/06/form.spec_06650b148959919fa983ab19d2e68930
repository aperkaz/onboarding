'use strict';var _form = require('../../client/reducers/form');var _form2 = _interopRequireDefault(_form);
var _campaigns = require('../../client/constants/campaigns');
var _campaignContacts = require('../../client/constants/campaignContacts');
var _forms = require('../../client/constants/forms');function _interopRequireDefault(obj) {return obj && obj.__esModule ? obj : { default: obj };}


describe('Form reducer', function () {
  it('Should handle CREATE_CAMPAIGN_FORM', function () {
    var initialState = { fields: {
        campaignId: {} } };

    var newState = (0, _form2.default)(initialState, { type: 'CREATE_CAMPAIGN_FORM' })(initialState, { type: 'CAMPAIGN_CREATE_ERROR' });

    expect({
      fields: {
        campaignId: {
          touched: true } },


      syncErrors: {
        campaignId: "campaignEditor.campaignForm.error.unique" } }).

    toEqual(newState);
  });
  // it('Should handle CREATE_CAMPAIGN_CONTACT_FORM', () => {
  //   const initialState = {fields: {
  //     campaignId: {}
  //   }};
  //   const newState = reducer(initialState, {type: 'CREATE_CAMPAIGN_CONTACT_FORM'});
  //   expect({
  //     fields: {
  //       campaignId: {
  //         touched: true
  //       }
  //     },
  //     syncErrors: {
  //       campaignId: "campaignEditor.campaignForm.error.unique"
  //     }
  //   }).toEqual(newState);
  // });
});